#include <stdio.h>
#include <ctype.h>
#include <math.h>
#include <locale.h>
#include <stdlib.h>
int main()
{
	int * x ;
	 x = (int *) malloc (3);
	 
	 float * Y;
	 float * Y = (float *) malloc ( * sizeof(float));
	 
	  scanf("Digite o numero de posicões do 1° vetor : ");
	  scanf("Digite o numero de posicões do 2° vetor : ");
	  
  for (int i = 0; i < *x; i++) {
     printf("\ Digite a quantidade de pecas %i produzidas pelo usuario %i",i ,  );
    }
	
    return 0;
}
